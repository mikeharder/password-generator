# password-generator
```
Usage:
  PasswordGenerator [options]

Options:
  --length <length>            length [default: 16]
  --min-symbols <min-symbols>  minSymbols [default: 2]
  --version                    Show version information
  -?, -h, --help               Show help and usage information
```
